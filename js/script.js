
$(document).ready(function() {
  // Quando clicco ne tasto Invio(send-button)
  $('.send-button').click(function() {

  });
  //
  function sendMessages() {
    var messageText = $('.send-messages-input input').val();
    if(messagetext) != '') {
      var newMessage = $('.templates .message').clone();
    }
  }
});
